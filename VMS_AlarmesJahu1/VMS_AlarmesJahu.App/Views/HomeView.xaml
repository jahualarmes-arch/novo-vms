<UserControl x:Class="VMS_AlarmesJahu.App.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Top area (similar to SmartPSS Home) -->
        <StackPanel Grid.Row="0" Margin="20">
            <!-- Seção Principal: Exibição ao Vivo e Reprodução -->
            <Grid Margin="0,10,0,30">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Exib. ao vivo - Estilo SmartPSS Lite -->
                <Button x:Name="BtnLiveView" Grid.Column="0" Margin="0,0,15,0" Height="140"
                        Background="{DynamicResource Card}" BorderBrush="{DynamicResource Border}" 
                        BorderThickness="1" Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" 
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="10" Padding="20">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <!-- Ícone de câmera estilizado -->
                                    <Border Grid.Column="0" Width="70" Height="70" CornerRadius="35" 
                                            Background="#1e3a5f" Margin="0,0,20,0">
                                        <TextBlock Text="🎥" FontSize="32" 
                                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock Text="Exib. ao vivo" FontSize="18" FontWeight="SemiBold" 
                                                   Foreground="{DynamicResource FgPrimary}"/>
                                        <TextBlock Text="A operação de visualização ao vivo do grupo de canais. Aqui é possível gravar, tirar instantâneos e realizar a operação PTZ." 
                                                   TextWrapping="Wrap" Opacity="0.7" Margin="0,6,0,0" FontSize="12"
                                                   Foreground="{DynamicResource FgPrimary}"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#2d3e50"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <!-- Reprodução -->
                <Button x:Name="BtnPlayback" Grid.Column="1" Margin="15,0,0,0" Height="140"
                        Background="{DynamicResource Card}" BorderBrush="{DynamicResource Border}" 
                        BorderThickness="1" Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" 
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="10" Padding="20">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <!-- Ícone de gravação -->
                                    <Border Grid.Column="0" Width="70" Height="70" CornerRadius="35" 
                                            Background="#3d2a1f" Margin="0,0,20,0">
                                        <TextBlock Text="⏯️" FontSize="32" 
                                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock Text="Reprodução" FontSize="18" FontWeight="SemiBold" 
                                                   Foreground="{DynamicResource FgPrimary}"/>
                                        <TextBlock Text="Pesquise e reproduza arquivos de vídeo remotos do grupo do canal, e exporte a operação." 
                                                   TextWrapping="Wrap" Opacity="0.7" Margin="0,6,0,0" FontSize="12"
                                                   Foreground="{DynamicResource FgPrimary}"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#2d3e50"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>

            <!-- Botão Fullscreen Limpo (destaque) -->
            <!-- WPF não aceita "Linear Gradient(...)" estilo CSS. Use o brush do tema (PrimaryGradient). -->
            <Border Background="{DynamicResource PrimaryGradient}"
                    CornerRadius="10" Padding="20" Margin="0,0,0,20" Cursor="Hand">
                <Button x:Name="BtnFullscreen" Background="Transparent" BorderThickness="0">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                
                                <TextBlock Grid.Column="0" Text="🖥️" FontSize="36" VerticalAlignment="Center" Margin="0,0,15,0"/>
                                
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="Abrir Mosaico Fullscreen" FontSize="16" FontWeight="SemiBold" Foreground="White"/>
                                    <TextBlock Text="Visualize todas as câmeras em tela cheia sem distrações (estilo SmartPSS)" 
                                               Foreground="#B0FFFFFF" FontSize="12" Margin="0,4,0,0"/>
                                </StackPanel>
                                
                                <TextBlock Grid.Column="2" Text="→" FontSize="24" Foreground="White" VerticalAlignment="Center"/>
                            </Grid>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Border>

            <Border Background="{DynamicResource Card}" BorderBrush="{DynamicResource Border}" BorderThickness="1" CornerRadius="10" Padding="14" Margin="0,0,0,0">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="💡" FontSize="16" Margin="0,0,10,0"/>
                    <TextBlock Text="Dica: No modo fullscreen, pressione ESC para sair ou números 1-8 para mudar o layout do mosaico." VerticalAlignment="Center"/>
                </StackPanel>
            </Border>
        </StackPanel>

        <!-- Bottom shortcuts (icon tiles) -->
        <Border Grid.Row="1" Background="{DynamicResource Panel}" BorderBrush="{DynamicResource Border}" BorderThickness="0,1,0,0" Padding="20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Gerenciamento" FontWeight="SemiBold" Margin="0,0,0,12"/>

                <UniformGrid Grid.Row="1" Columns="4" Rows="1" HorizontalAlignment="Left">
                    <Button x:Name="BtnDeviceManager" Style="{DynamicResource QuickTileBtn}" Margin="0,0,12,0">
                        <StackPanel>
                            <TextBlock Text="📦" FontSize="28" HorizontalAlignment="Center"/>
                            <TextBlock Text="Gerenciador do&#x0a;Dispositivo" TextAlignment="Center" Margin="0,10,0,0"/>
                        </StackPanel>
                    </Button>
                    <Button x:Name="BtnLogs" Style="{DynamicResource QuickTileBtn}" Margin="0,0,12,0">
                        <StackPanel>
                            <TextBlock Text="📋" FontSize="28" HorizontalAlignment="Center"/>
                            <TextBlock Text="Consulta de&#x0a;Registro" TextAlignment="Center" Margin="0,10,0,0"/>
                        </StackPanel>
                    </Button>
                    <Button x:Name="BtnEventConfig" Style="{DynamicResource QuickTileBtn}" Margin="0,0,12,0">
                        <StackPanel>
                            <TextBlock Text="🚨" FontSize="28" HorizontalAlignment="Center"/>
                            <TextBlock Text="Config. de&#x0a;Evento" TextAlignment="Center" Margin="0,10,0,0"/>
                        </StackPanel>
                    </Button>
                    <Button x:Name="BtnManual" Style="{DynamicResource QuickTileBtn}">
                        <StackPanel>
                            <TextBlock Text="📖" FontSize="28" HorizontalAlignment="Center"/>
                            <TextBlock Text="Manual do&#x0a;Usuário" TextAlignment="Center" Margin="0,10,0,0"/>
                        </StackPanel>
                    </Button>
                </UniformGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
