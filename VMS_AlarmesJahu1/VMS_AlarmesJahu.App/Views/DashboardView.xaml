<UserControl x:Class="VMS_AlarmesJahu.App.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="32">
        <StackPanel>
            <!-- Header Moderno -->
            <Grid Margin="0,0,0,32">
                <StackPanel>
                    <TextBlock Style="{DynamicResource TitleText}" Foreground="{DynamicResource FgPrimary}">
                        <Run Text="Dashboard"/>
                    </TextBlock>
                    <TextBlock Style="{DynamicResource SubtitleText}" Margin="0,4,0,0">
                        <Run Text="Visão geral do sistema • Atualizado agora"/>
                    </TextBlock>
                </StackPanel>
                <Button Content="🔄 Atualizar" HorizontalAlignment="Right" VerticalAlignment="Center" Padding="16,10"/>
            </Grid>

            <!-- Cards de Estatísticas Modernos -->
            <Grid Margin="0,0,0,24">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Card 1: Eventos Hoje -->
                <Border Grid.Column="0" Margin="0,0,12,0" CornerRadius="20" Padding="24" BorderThickness="1" BorderBrush="{DynamicResource Border}">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#1A2332" Offset="0"/>
                            <GradientStop Color="#1F2937" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>
                    <Border.Effect>
                        <DropShadowEffect Color="#000000" BlurRadius="20" ShadowDepth="0" Opacity="0.3"/>
                    </Border.Effect>
                    <StackPanel>
                        <!-- Ícone Grande -->
                        <Border Width="56" Height="56" CornerRadius="16" HorizontalAlignment="Left" Margin="0,0,0,16">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="#3B82F6" Offset="0"/>
                                    <GradientStop Color="#2563EB" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            <TextBlock Text="📊" FontSize="28" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        
                        <TextBlock Text="Eventos Hoje" Style="{DynamicResource MutedText}" Margin="0,0,0,8"/>
                        <TextBlock Text="{Binding EventsToday}" FontSize="36" FontWeight="Bold" Foreground="{DynamicResource FgPrimary}"/>
                        
                        <!-- Badge de mudança -->
                        <Border Background="#10B98120" CornerRadius="6" Padding="8,4" HorizontalAlignment="Left" Margin="0,12,0,0">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="↗" Foreground="#10B981" FontSize="12" Margin="0,0,4,0"/>
                                <TextBlock Text="+12% vs ontem" Foreground="#10B981" FontSize="11"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Border>

                <!-- Card 2: Esta Semana -->
                <Border Grid.Column="1" Margin="6,0" CornerRadius="20" Padding="24" BorderThickness="1" BorderBrush="{DynamicResource Border}">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#1A2332" Offset="0"/>
                            <GradientStop Color="#1F2937" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>
                    <Border.Effect>
                        <DropShadowEffect Color="#000000" BlurRadius="20" ShadowDepth="0" Opacity="0.3"/>
                    </Border.Effect>
                    <StackPanel>
                        <Border Width="56" Height="56" CornerRadius="16" HorizontalAlignment="Left" Margin="0,0,0,16">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="#8B5CF6" Offset="0"/>
                                    <GradientStop Color="#7C3AED" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            <TextBlock Text="📅" FontSize="28" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        
                        <TextBlock Text="Esta Semana" Style="{DynamicResource MutedText}" Margin="0,0,0,8"/>
                        <TextBlock Text="{Binding EventsWeek}" FontSize="36" FontWeight="Bold" Foreground="{DynamicResource FgPrimary}"/>
                        
                        <Border Background="#8B5CF620" CornerRadius="6" Padding="8,4" HorizontalAlignment="Left" Margin="0,12,0,0">
                            <TextBlock Text="Últimos 7 dias" Foreground="#8B5CF6" FontSize="11"/>
                        </Border>
                    </StackPanel>
                </Border>

                <!-- Card 3: Este Mês -->
                <Border Grid.Column="2" Margin="6,0" CornerRadius="20" Padding="24" BorderThickness="1" BorderBrush="{DynamicResource Border}">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#1A2332" Offset="0"/>
                            <GradientStop Color="#1F2937" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>
                    <Border.Effect>
                        <DropShadowEffect Color="#000000" BlurRadius="20" ShadowDepth="0" Opacity="0.3"/>
                    </Border.Effect>
                    <StackPanel>
                        <Border Width="56" Height="56" CornerRadius="16" HorizontalAlignment="Left" Margin="0,0,0,16">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="#06B6D4" Offset="0"/>
                                    <GradientStop Color="#0891B2" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            <TextBlock Text="📆" FontSize="28" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        
                        <TextBlock Text="Este Mês" Style="{DynamicResource MutedText}" Margin="0,0,0,8"/>
                        <TextBlock Text="{Binding EventsMonth}" FontSize="36" FontWeight="Bold" Foreground="{DynamicResource FgPrimary}"/>
                        
                        <Border Background="#06B6D420" CornerRadius="6" Padding="8,4" HorizontalAlignment="Left" Margin="0,12,0,0">
                            <TextBlock Text="Últimos 30 dias" Foreground="#06B6D4" FontSize="11"/>
                        </Border>
                    </StackPanel>
                </Border>

                <!-- Card 4: Dispositivos -->
                <Border Grid.Column="3" Margin="6,0" CornerRadius="20" Padding="24" BorderThickness="1" BorderBrush="{DynamicResource Border}">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#1A2332" Offset="0"/>
                            <GradientStop Color="#1F2937" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>
                    <Border.Effect>
                        <DropShadowEffect Color="#000000" BlurRadius="20" ShadowDepth="0" Opacity="0.3"/>
                    </Border.Effect>
                    <StackPanel>
                        <Border Width="56" Height="56" CornerRadius="16" HorizontalAlignment="Left" Margin="0,0,0,16">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="#10B981" Offset="0"/>
                                    <GradientStop Color="#059669" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            <TextBlock Text="📡" FontSize="28" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        
                        <TextBlock Text="Dispositivos" Style="{DynamicResource MutedText}" Margin="0,0,0,8"/>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding ConnectedDevices}" FontSize="36" FontWeight="Bold" Foreground="#10B981"/>
                            <TextBlock Text="/" FontSize="36" FontWeight="Bold" Foreground="{DynamicResource FgMuted}" Margin="4,0"/>
                            <TextBlock Text="{Binding TotalDevices}" FontSize="36" FontWeight="Bold" Foreground="{DynamicResource FgMuted}"/>
                        </StackPanel>
                        
                        <Border Background="#10B98120" CornerRadius="6" Padding="8,4" HorizontalAlignment="Left" Margin="0,12,0,0">
                            <TextBlock Text="Online" Foreground="#10B981" FontSize="11" FontWeight="SemiBold"/>
                        </Border>
                    </StackPanel>
                </Border>

                <!-- Card 5: Regras Ativas -->
                <Border Grid.Column="4" Margin="12,0,0,0" CornerRadius="20" Padding="24" BorderThickness="1" BorderBrush="{DynamicResource Border}">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#1A2332" Offset="0"/>
                            <GradientStop Color="#1F2937" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>
                    <Border.Effect>
                        <DropShadowEffect Color="#000000" BlurRadius="20" ShadowDepth="0" Opacity="0.3"/>
                    </Border.Effect>
                    <StackPanel>
                        <Border Width="56" Height="56" CornerRadius="16" HorizontalAlignment="Left" Margin="0,0,0,16">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="#F59E0B" Offset="0"/>
                                    <GradientStop Color="#D97706" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            <TextBlock Text="🧠" FontSize="28" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        
                        <TextBlock Text="Regras Ativas" Style="{DynamicResource MutedText}" Margin="0,0,0,8"/>
                        <TextBlock Text="{Binding ActiveRules}" FontSize="36" FontWeight="Bold" Foreground="{DynamicResource FgPrimary}"/>
                        
                        <Border Background="#F59E0B20" CornerRadius="6" Padding="8,4" HorizontalAlignment="Left" Margin="0,12,0,0">
                            <TextBlock Text="IA Operando" Foreground="#F59E0B" FontSize="11" FontWeight="SemiBold"/>
                        </Border>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Conteúdo Principal -->
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Eventos Recentes - Estilo Moderno -->
                <Border Grid.Column="0" Margin="0,0,16,0" CornerRadius="20" Padding="28" BorderThickness="1" BorderBrush="{DynamicResource Border}">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                            <GradientStop Color="#1A2332" Offset="0"/>
                            <GradientStop Color="#151E2B" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>
                    <Border.Effect>
                        <DropShadowEffect Color="#000000" BlurRadius="20" ShadowDepth="0" Opacity="0.3"/>
                    </Border.Effect>
                    
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        
                        <!-- Header da Seção -->
                        <Grid Grid.Row="0" Margin="0,0,0,20">
                            <StackPanel>
                                <TextBlock Text="🚨 Eventos Recentes" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource FgPrimary}"/>
                                <TextBlock Text="Últimas detecções do sistema" Style="{DynamicResource MutedText}" Margin="0,4,0,0"/>
                            </StackPanel>
                            <Button Content="Ver Todos →" HorizontalAlignment="Right" VerticalAlignment="Center" Padding="12,8" FontSize="12"/>
                        </Grid>
                        
                        <ListView Grid.Row="1" ItemsSource="{Binding RecentEvents}" 
                                  Background="Transparent" BorderThickness="0"
                                  ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="{DynamicResource Card}" CornerRadius="12" Padding="16" Margin="0,0,0,12" BorderThickness="1" BorderBrush="{DynamicResource Border}">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            
                                            <!-- Status Badge -->
                                            <Border Grid.Column="0" Width="48" Height="48" CornerRadius="12" Margin="0,0,16,0" VerticalAlignment="Center">
                                                <Border.Background>
                                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                        <GradientStop Color="#EF444430" Offset="0"/>
                                                        <GradientStop Color="#DC262620" Offset="1"/>
                                                    </LinearGradientBrush>
                                                </Border.Background>
                                                <TextBlock Text="⚠️" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            
                                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                <TextBlock FontWeight="SemiBold" FontSize="14" Foreground="{DynamicResource FgPrimary}">
                                                    <Run Text="{Binding DeviceName}"/>
                                                    <Run Text=" • "/>
                                                    <Run Text="{Binding ChannelDisplay}"/>
                                                </TextBlock>
                                                <TextBlock Foreground="{DynamicResource FgSecondary}" FontSize="12" Margin="0,4,0,0">
                                                    <Run Text="{Binding ClassName}"/>
                                                    <Run Text=" detectado em "/>
                                                    <Run Text="{Binding RuleName}"/>
                                                </TextBlock>
                                            </StackPanel>
                                            
                                            <!-- Time Badge -->
                                            <Border Grid.Column="2" Background="{DynamicResource Panel}" CornerRadius="8" Padding="12,6" VerticalAlignment="Center">
                                                <TextBlock Text="{Binding TimeDisplay}" Foreground="{DynamicResource FgSecondary}" FontSize="11" FontWeight="Medium"/>
                                            </Border>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </Grid>
                </Border>

                <!-- Painel Lateral Modernizado -->
                <StackPanel Grid.Column="1">
                    <!-- Top Câmeras -->
                    <Border Margin="0,0,0,16" CornerRadius="20" Padding="24" BorderThickness="1" BorderBrush="{DynamicResource Border}">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                <GradientStop Color="#1A2332" Offset="0"/>
                                <GradientStop Color="#151E2B" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.Background>
                        <Border.Effect>
                            <DropShadowEffect Color="#000000" BlurRadius="20" ShadowDepth="0" Opacity="0.3"/>
                        </Border.Effect>
                        
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                                <Border Width="40" Height="40" CornerRadius="10" Margin="0,0,12,0">
                                    <Border.Background>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                            <GradientStop Color="#0066CC30" Offset="0"/>
                                            <GradientStop Color="#0052A320" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Border.Background>
                                    <TextBlock Text="📹" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <StackPanel VerticalAlignment="Center">
                                    <TextBlock Text="Top Câmeras" FontWeight="SemiBold" FontSize="15" Foreground="{DynamicResource FgPrimary}"/>
                                    <TextBlock Text="Últimos 7 dias" Style="{DynamicResource MutedText}" FontSize="11"/>
                                </StackPanel>
                            </StackPanel>
                            
                            <ItemsControl ItemsSource="{Binding TopChannels}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Margin="0,8">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            
                                            <StackPanel Grid.Column="0">
                                                <TextBlock Text="{Binding Label}" Foreground="{DynamicResource FgPrimary}" FontSize="13"/>
                                                <!-- Barra de progresso -->
                                                <Border Height="6" Background="{DynamicResource Panel}" CornerRadius="3" Margin="0,6,16,0">
                                                    <Border Background="{DynamicResource PrimaryGradient}" CornerRadius="3" HorizontalAlignment="Left" Width="80"/>
                                                </Border>
                                            </StackPanel>
                                            
                                            <Border Grid.Column="1" Background="{DynamicResource PrimaryGradient}" CornerRadius="8" Padding="10,6" VerticalAlignment="Center">
                                                <TextBlock Text="{Binding Count}" FontWeight="Bold" FontSize="14" Foreground="White"/>
                                            </Border>
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>

                    <!-- Top Classes -->
                    <Border Margin="0,0,0,16" CornerRadius="20" Padding="24" BorderThickness="1" BorderBrush="{DynamicResource Border}">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                <GradientStop Color="#1A2332" Offset="0"/>
                                <GradientStop Color="#151E2B" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.Background>
                        <Border.Effect>
                            <DropShadowEffect Color="#000000" BlurRadius="20" ShadowDepth="0" Opacity="0.3"/>
                        </Border.Effect>
                        
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                                <Border Width="40" Height="40" CornerRadius="10" Margin="0,0,12,0">
                                    <Border.Background>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                            <GradientStop Color="#8B5CF630" Offset="0"/>
                                            <GradientStop Color="#7C3AED20" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Border.Background>
                                    <TextBlock Text="🏷️" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <StackPanel VerticalAlignment="Center">
                                    <TextBlock Text="Top Classes" FontWeight="SemiBold" FontSize="15" Foreground="{DynamicResource FgPrimary}"/>
                                    <TextBlock Text="Objetos detectados" Style="{DynamicResource MutedText}" FontSize="11"/>
                                </StackPanel>
                            </StackPanel>
                            
                            <ItemsControl ItemsSource="{Binding TopClasses}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="{DynamicResource Card}" CornerRadius="10" Padding="12" Margin="0,6">
                                            <Grid>
                                                <TextBlock Text="{Binding ClassName}" Foreground="{DynamicResource FgPrimary}" FontSize="13" VerticalAlignment="Center"/>
                                                <Border Background="{DynamicResource AccentGradient}" CornerRadius="6" Padding="8,4" HorizontalAlignment="Right">
                                                    <TextBlock Text="{Binding Count}" FontWeight="Bold" FontSize="12" Foreground="White"/>
                                                </Border>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>

                    <!-- Status do Sistema -->
                    <Border CornerRadius="20" Padding="24" BorderThickness="1" BorderBrush="{DynamicResource Border}">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                <GradientStop Color="#1A2332" Offset="0"/>
                                <GradientStop Color="#151E2B" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.Background>
                        <Border.Effect>
                            <DropShadowEffect Color="#000000" BlurRadius="20" ShadowDepth="0" Opacity="0.3"/>
                        </Border.Effect>
                        
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                                <Border Width="40" Height="40" CornerRadius="10" Margin="0,0,12,0">
                                    <Border.Background>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                            <GradientStop Color="#10B98130" Offset="0"/>
                                            <GradientStop Color="#05966920" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Border.Background>
                                    <TextBlock Text="ℹ️" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <StackPanel VerticalAlignment="Center">
                                    <TextBlock Text="Status Sistema" FontWeight="SemiBold" FontSize="15" Foreground="{DynamicResource FgPrimary}"/>
                                    <TextBlock Text="Monitoramento ativo" Style="{DynamicResource MutedText}" FontSize="11"/>
                                </StackPanel>
                            </StackPanel>
                            
                            <Border Background="{DynamicResource Card}" CornerRadius="10" Padding="12" Margin="0,6">
                                <Grid>
                                    <TextBlock Text="Último evento" Foreground="{DynamicResource FgSecondary}" FontSize="12"/>
                                    <TextBlock Text="{Binding LastEventTime}" Foreground="{DynamicResource FgPrimary}" FontSize="12" FontWeight="Medium" HorizontalAlignment="Right"/>
                                </Grid>
                            </Border>
                            
                            <Border Background="{DynamicResource Card}" CornerRadius="10" Padding="12" Margin="0,6">
                                <Grid>
                                    <TextBlock Text="Uptime IA" Foreground="{DynamicResource FgSecondary}" FontSize="12"/>
                                    <TextBlock Text="{Binding Uptime}" Foreground="{DynamicResource FgPrimary}" FontSize="12" FontWeight="Medium" HorizontalAlignment="Right"/>
                                </Grid>
                            </Border>
                            
                            <Border Background="{DynamicResource Card}" CornerRadius="10" Padding="12" Margin="0,6">
                                <Grid>
                                    <TextBlock Text="IA Status" Foreground="{DynamicResource FgSecondary}" FontSize="12"/>
                                    <Border Background="#10B98130" CornerRadius="6" Padding="8,4" HorizontalAlignment="Right">
                                        <TextBlock Text="● Online" Foreground="#10B981" FontSize="11" FontWeight="SemiBold"/>
                                    </Border>
                                </Grid>
                            </Border>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>
        </StackPanel>
    </ScrollViewer>
</UserControl>
